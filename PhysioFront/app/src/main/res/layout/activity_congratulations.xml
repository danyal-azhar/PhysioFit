<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/background_dotted"
    tools:context="com.balychev.tripleimpact.CongratulationsActivity">

    <ImageView
        android:id="@+id/c_header_image"
        android:layout_width="280sp"
        android:layout_height="40sp"
        android:src="@drawable/congratulations"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/c_description_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/workout_finished"
        android:textColor="#ffffff"
        android:textSize="20dp"
        android:layout_below="@+id/c_header_image"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageView
        android:id="@+id/c_hero_image"
        android:layout_width="250sp"
        android:layout_height="250sp"
        android:src="@drawable/hero"
        android:layout_marginTop="20sp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/c_description_label"
        />

    <Button
        android:id="@+id/w_done_button"
        style="@style/homeButton"
        android:background="@drawable/button_green"
        android:onClick="onDoneClick"
        android:text="@string/done"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="bottom"
        android:textColor="#ffffff" />
</RelativeLayout>
